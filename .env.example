# Copy to .env and fill in values (never commit .env)

MONGO_URI=
AUTH_SECRET=
UPLOADTHING_TOKEN=

# Optional — production base URL for invite links
# AUTH_URL=http://localhost:3000

# OTP email (Gmail: use an app password, not your normal password)
EMAIL_SERVICE=gmail
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
